// Home feature: the public landing page (route `/`) presenting the product to
// signed-out visitors. Logged-in users are redirected to /dashboard by the
// auth guard, so this page is only ever seen anonymously.
export default defineNuxtConfig({})
