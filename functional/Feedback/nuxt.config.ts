// Feedback feature: a SHARED feedback board (Canny / GitHub-style) — unlike the
// rest of the app (private per user). Users post, upvote and reply; an ADMIN
// moderates each entry's status. All scoping/counts come from the API.
export default defineNuxtConfig({})
