// Admin feature: pages réservées aux administrateurs (rôle ADMIN sur /me).
// Pour l'instant : import de recettes dans le catalogue global (fichier .xlsx
// scrapé en local, ou liste d'URLs scrapées côté serveur).
export default defineNuxtConfig({})
